<h2>User info</h2>
 <%= attachment_image_tag user, :image, :fill, 50, 50, fallback: 'no-image.jpg', :size => '50x50' %>

<table class="table">
	<tr>
		<th>name</th>
		<th><%= user.name %></th>
	</tr>
	<br>
	<tr>
		<th>introduction</th>
		<th><%= user.introduction %></th>
	</tr>
</table>


<br>
<%= link_to "", edit_user_path(user.id), class:"btn btn-default glyphicon glyphicon-wrench edit_user_#{user.id}" %>

